

const darkMode = {
  background: gray,
  body: blue

};

const lightMode = {

  background: white,
  body: black
};

const theme = mode => (mode === "dark" ? darkMode : lightMode);

const







export default theme
